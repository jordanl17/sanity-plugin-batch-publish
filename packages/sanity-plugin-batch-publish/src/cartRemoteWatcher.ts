/**
 * Stub — implementation will be added in the GREEN commit.
 *
 * @internal
 */
export function createCartRemoteWatcher(_params: {
  documentStore: unknown
  cartStore: unknown
  currentUserId: string
}): {stop(): void} {
  return {stop(): void {}}
}
